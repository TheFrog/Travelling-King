public class Edge {
	// - length : float
	// - target : Node
	// - vehicle: Vehicle
	
	// Pre: l >= 0 and t != null.
	// Post: has created an Edge with length l, target t and vehicle v.
	// + Edge(l : float, t : Node, v : Vehicle)
	
	// Post: returns cost (= length / vehicle.getSpeed()).
	// + getCost() : float
	
	// Post: returns target.
	// + getTarget() : Node
	
	// Post: returns vehicle.
	// + getVehicle() : Vehicle
}